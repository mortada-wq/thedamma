kind = "web"
previewPath = "/"
title = "SongForge"
version = "1.0.0"
id = "artifacts/songforge"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21665

[services.development]
run = "pnpm --filter @workspace/songforge run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/songforge", "run", "build" ]
publicDir = "artifacts/songforge/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21665"
BASE_PATH = "/"
